# Deferred Backlog

スコープ外・保留にした課題の台帳（append-only）。完了報告の前にここへの記録が必須。

## 2026-08-07 (B0 土台バッチ)

- **8 Conlay (YOO8) は管財人管理下（receivership）**: 2025-2026にKSK Landと施工会社の紛争で管財人管理・売却手続き中。引き渡しゼロ。CSVは year=2026 / status=upcoming としたが「頓挫」を表す状態値がない。状態モデル拡張（stalled等）はB3以降で検討。出典: The Edge Malaysia (768919, 791494)。
- **premium_score 11件はフラグから機械的に再計算しただけ**: 個々のフラグ（pool等）自体の外部再検証はしていない。フラグの再検証は別トラック（データ精度向上）で。手順は memory の condo-verification 参照（pool=0 は73%誤りの実績あり）。
- **condos homepage_url が 68% (185/271) 空**: データ精度トラックで補完。
- **schools_detail.json はペナン9校のみ**: KL 24校の詳細（学費表・国籍構成等）が無い。データ精度トラックで。
- **GitHub Pages は「静的直配信」を維持**: Viteビルドを挟むデプロイ（actions/deploy-pages）への切替は、ビルドが必要になった時点で判断。現状は不要。
- **iproperty_url は検証していない**: 403で直接fetch不可のため。データ精度トラックで search-snippet 方式により確認。

## 2026-08-07 (竹森氏裁定: データ精査トラック中止)

- **コンドミニアム271件のデータ精査（フラグ再検証・homepage_url補完・iproperty_url確認）は今回スコープ外**（竹森氏 2026-08-07 明示: アプリのブラッシュアップがメイン）。将来再開する場合の手順はメモリの condo/commercial/address verification 3本に保存済み。
- KL校23校の詳細（schools_detail.json拡充）のみ、表示格差解消のため実行（進行中）。追加のデータ調査は出さない。

## 2026-08-07 (B1 モジュール分割で発見された既存バグ・意図的に未修正)

- **STATUSフィルタ「Residential Only」が常に0件**: 商業/学校は自分の分岐で落ち、condoは `status !== 'residential'` で全滅する既存バグ。B3で status フィルタから residential/commercial 選択肢自体を撤去予定（層タブが代替）のため、いま直さない。test/filter.test.js に KNOWN BUG として文書化済み。
- **School Finder の近隣物件クリックの挙動が不自然**: `sfActive=false; toggleSchoolFinder()` が反転して panel が開いたままになり、一度ペナン全景に飛んでから物件へ。B3の周辺エンジン刷新で解消予定。
- B1のUIモジュールは使い捨て等価性ハーネス（1,250万バイトのHTML出力一致・15.7万ケースのフィルタ一致）で検証したが、恒久のDOMレベル回帰テストは未整備。B2以降で必要になれば jsdom 導入を検討。

## 2026-08-07 (B3a 情報設計で保留にしたもの)

- **ペナンのエリア未分類が27件**: AREAに追加した4エリア（Gurney/Tanjung/Ferringhi/Bayan）に、Gelugor・Jelutong・George Town中心部のコンド27件が入らない。ジャンプバーにも該当ボタンが無いため、まずジャンプバー側にエリアを足すか判断が要る（AREAとジャンプバーは同じキーで揃える方針）。現状は「エリア=すべて」で到達可能。
- **帯フィルタの境界が両端included**: 学費「≤40K」と「40K-80K」は40,000ちょうどの学校を両方が拾う（NLA・PSF・家賃も同様）。既存のPSF/家賃の帯と同じ挙動に揃えたもの。半開区間に直すなら全帯を一斉に。
- **upcoming物件のカードは「価格 未定」のまま**: CSVには rentMin/salePsfMin の値が入っているが、既存仕様（未完成物件の価格は出さない）を継承した。→ **2026-08-07 竹森氏裁定: 非表示のままで確定（クローズ）**。
- **サマリー上部のヘッダー重複（監査A2）・タイトル改名（A1）は未着手**: B3bの視覚システム側の担当。
- **URL状態（?layer=&sel=）と詳細パネルの「周辺」タブは B3a-1 のスコープ外**: 別バッチで実装。
- **DOMレベルの回帰テストは今回も未整備**: カード生成は純関数に切り出して test/card.test.js でカバーしたが、フィルタ↔DOMの結線（層切替でどのコントロールが出るか等）は実画面確認に依存。

## 2026-08-07 (B3a-2 URL状態＋周辺タブで保留にしたもの)

- **School Finder の「3km以内のコンド」は nearby() に載せ替えていない**: B3仕様は「このエンジンの一利用形に昇格（UIは維持）」としているが、今回のスコープが School Finder 非改変だったため独自の haversine 呼び出しのまま。上の B1 で記録した「近隣物件クリックの挙動が不自然」も未解消。B3c の「学費くらべ」改組と同時にやるのが自然。
- **フィルタ値はURLに入れていない**: B3仕様どおり（`?layer=&sel=&tab=` のみ）。共有リンクで絞り込み条件まで再現したいかは B4 で裁定。
- **周辺タブは「バケット×種別で上位5件」で打ち止め**: 徒歩圏に物件29件あっても5件しか出ない。「もっと見る」は付けていない（280pxパネルの可読性優先）。件数行には実数が出るので取りこぼしは可視。
- **詳細パネルの視覚的な作り直しは未着手**: 種別色の細帯・Tier/種別チップ・44pxの閉じる標的・外部リンクのテキストボタン統一は B3b（2.6）の担当。今回はタブを足しただけで、詳細タブの中身は B3a-1 のまま。
- **DOMレベルの回帰テストは今回も未整備**: nearby()・URLの読み書きは純関数として test/nearby.test.js（19件）と test/urlState.test.js（20件）でカバーしたが、「タブを押す→URLがreplaceされる」等の結線は実画面確認に依存。

## 2026-08-07 (B3c 学費くらべ＋磨き)

**解消した既存項目**（上のB1・B3a-2で記録したもの）
- ✅ **School Finder の近隣物件クリックの挙動が不自然**: `sfActive=false;toggleSchoolFinder()` のインライン代入を廃止し、`sfSelectCondo()`（パネルを閉じる→層を物件に切替→選択）に置換。`window.sfActive` アクセサも削除。開いた瞬間にペナン全景へ飛ぶ `map.setView` も撤去（KL校を含むようになったため、飛ぶ先が存在しない）。
- ✅ **School Finder の「3km以内のコンド」を nearby() に載せ替え**: 独自 haversine を撤去し、詳細パネルの周辺タブと同じエンジンへ。バケットは 800m / 2km / 3km（通学圏に合わせて外周だけ 6km→3km）。「近い」の定義がアプリ内で一つになった。

**今回スコープ外にしたもの**
- **学費チャート（SVG曲線）はペナン9校のまま**: 曲線を引くには3〜17歳の全年齢に値が要るが、KL23校の公表は代表学年のみ（例: ISKL は Prep Reception / Prep Senior / Grade 6 / Grade 12 の4点）。曲線を作ると点と点の間を捏造することになるため、KL校は一覧（実額＋学年ラベル）だけで扱う。タイトルも「ペナン9校・年齢別」に改めて誤認を防止済み。KL側を出すなら「点だけを打つ散布図」が候補。
- **`SF_FEES`（inline.js の年齢別テーブル）と `schools_detail.json` が二重管理**: チャートは前者、一覧は後者を読む。全15年齢×ペナン9校を突き合わせた結果、食い違いは **2点だけ**（Fairview ペナン校の6歳＝20,000 vs 25,000、8歳＝25,000 vs 32,000）。原因は SF_FEES が PYP の学年を1年ずらして埋めていたこと。学校自身が `PYP 1 (Year 1)` と書いている以上 PYP N＝Year N が正なので、一覧側（公表実額）が正しい。チャートを廃止するか SF_FEES を実額に揃えるかは要裁定。
- **年齢セレクトは3〜17歳のまま**: schools_data.csv の age_range は18まであるが、学年ラダーが Year 13 / Grade 12＝17歳で終わるため。18歳（大学準備年の一部）を足すかは要裁定。
- **Prep Senior / Kindergarten 1 など就学前ラベルの学年対応は「ラベルの字面どおり」**: K1=4歳・K2/K3=5歳・Prep Senior=5歳と解釈した。学校ごとに実際の対象年齢が半年〜1年ずれる可能性はあるが、ずれた場合も「※近い学年 〇〇 の実額」と明示されるため嘘の数字にはならない。各校サイトでの裏取りはデータ精度トラック。
- **DOMレベルの回帰テストは今回も未整備**: fees.js は純関数として test/fees.test.js（45件）でカバーし、実データ33校に対する「公表されていない数字を返さない」不変条件も検証済み。ただし「学費くらべを開く→年齢を変える→行をクリック」の結線は実画面確認（Chrome, localhost:5205）に依存。

## 2026-08-07 (B4 仕上げ: a11y・モバイル・OGP・資料整合)

**解消した既存項目**
- ✅ **匿名ボタンの解消**: 「押せるのに名前が無い」要素を全て潰した。カード・周辺行・学費くらべ行は `role="button" tabindex="0" aria-label`、パネル開閉ボタン（中身が `&#9664;` の矢印だけ＝名前ゼロ）と凡例に aria-label、地図マーカーとクラスタの divIcon にも aria-label。test/a11y.test.js が「visible text か aria-label のどちらかを全 button が持つ」を回帰として押さえる。
- ✅ **モバイルの高さが header 分だけはみ出していた**: `#map:73vh + .panel:27vh = 100vh` を、高さが `calc(100vh - 52px)` の `.main` の中に置いていたため、パネル下端がちょうどヘッダー高さ分クリップされていた。`.main` 自体の高さを `calc(100dvh - 44px)` に直し、分割を vh でなく flex の割合（パネル 45%・地図が残り）に変更。

**今回スコープ外にしたもの**
- **og:image は作っていない**: リポジトリにスクリーンショット資産が無く、それらしい画像を生成すると「このアプリではない何かの絵」を配ることになる。`twitter:card` は `summary`（画像なしカード）。OGP画像を出すなら、実画面のスクリーンショットを資産として commit してから。
- **地図マーカーのキーボード操作は実装していない**: Leaflet のマーカーは地理順に並んだ絶対配置 div で、Tab で辿ると地図上をランダムに飛び回る（最大392個）。同じレコードを意味のある順序で持つ一覧が代替経路になっており、`#map` の aria-label もそこへ誘導している。方針は src/ui/a11y.js の冒頭コメントに明記済み。マーカー個別のフォーカスが要るなら、Leaflet の keyboard plugin か「表示中マーカーだけを roving tabindex で辿る」独自実装が必要。
- **検索欄には aria-label を足していない**: 既に `<label for="fSearch">検索 / Search</label>` が accessible name になっており、別文言の aria-label を足すと可視ラベルと読み上げ名が食い違う（WCAG 2.5.3 label-in-name 違反）。「全フォーム要素が label か aria-label のどちらかを持つ」ことは test/a11y.test.js が検査する。
- **エリアジャンプのボタンはモバイル 32px**: 40px にすると地図上のバーが2行になり、地図そのものを潰す。13個が横スクロールで詰まる構造のため、40px 化は「バーを折りたたむ / ドロップダウン化」とセットでないと成立しない。要裁定。
- **フィルタチップの ✕ は擬似要素で 40px の当たり判定を作っている**: グリフを 40px にするとチップがピルでなくなるため。隣のチップのラベル部分と当たり判定が数px重なる可能性がある（✕同士は重ならない）。実機で誤タップが出るならチップ間の gap を広げる。
- **DOMレベルの回帰テストは今回も未整備**: initA11y のキー処理は依存注入（`{doc, onEscape}`）で手書きスタブから直接叩いて検証したが、ランドマーク・aria・モバイルCSSは index.html をテキストとして読む契約テストであり、実際の描画・フォーカスリングの見え方は実画面確認に依存。
- **URLにフィルタ値を載せるかは未裁定のまま**（B3a-2 で B4 送りとした項目）: 今回は a11y・モバイル・資料が主題だったため触っていない。

## 2026-08-07 (ペナン エリア追加: George Town / Gelugor)

**解消した既存項目**
- ✅ **ペナンのエリア未分類が27件**（B3a で保留）: 実データを数え直すと **26件**（27は当時の概算）。`George Town`（市中心部6件）と `Gelugor / Jelutong`（南東回廊19件）の2エリアをジャンプバー・エリアフィルタ・`matchesArea()` に追加し、Sungai Ara の1件（Skycube Residence）は隣接する `Bayan Lepas` に寄せた。これで**ペナンのコンド75件すべてがちょうど1エリアに入る**（gurney 22 / tanjung 14 / ferringhi 6 / bayan 8 / george-town 6 / gelugor 19）。既存4エリアの所属は1件も動いていない（bayan だけ 7→8 に増加）。`test/filter.test.js` が condos_data.csv を実際に走査して「未分類0・重複0」を回帰として押さえる。

**今回スコープ外にしたもの**
- **「George Town」は市全体の行政名なので、除外方式で定義した**: Gurney / Pulau Tikus の住所にも "George Town" が入る（22件中20件）ため、`george-town` は「他のペナンエリアに入らなかった George Town 住所」と定義している（KL の Mont Kiara と同じ考え方）。将来 Tanjung 側の住所に "George Town" が入っても取り違えない。
- **Paya Terubong / Air Itam は Gelugor / Jelutong に含めた**: 内陸の谷で、回廊から直接つながるため。厳密には別地区なので、件数が増えたら独立エリアに切り出す判断が要る（現状コンド1件・商業1件）。
- **学校・商業のペナン本土（Seberang Perai 側）はどのエリアにも入らない**: Batu Kawan の IKEA / Design Village、Butterworth の Penang Sentral など**商業7件**は島ではなく本土にあり、6エリアはすべて島内。`Balik Pulau` の POWIIS **学校1件**も島の西側でどのエリアにも属さない。本土エリア（Seberang Perai）を足すかは要裁定。コンドは全件が島内なので影響なし。
- **エリアジャンプのボタンが13個→15個になった**: B4 で記録した「モバイルでバーが横スクロールで詰まる（40px化するには折りたたみ/ドロップダウン化が必要）」がその分きつくなっている。要裁定の項目は変わらない。

## 2026-08-07 (D3 住まいモードの飲食レイヤー)

**今回スコープ外にしたもの**
- **D4（外食モード・個人記録UI）は一切作っていない**: 訪問済トグル / 再訪3択 / 実額入力 / 感想（複数行）/ 訪問済み一覧 / localStorage（`mkd_dining_personal_v1`）/ エクスポート・インポート。台帳v9の必須要件①②③④⑤はこの層に入るため、**D3時点では「飲食店を探す」だけで「記録する」はできない**。次バッチで実装する前提の分割であり、落としたわけではない。
- **座標が無い飲食店（`lat: null` + `geoPrecision: 'pending'`）は読み込み時に落としている**: `parseRestaurants()` の最後の `.filter()` 一箇所だけで落ちる。**現在は該当0件**なので無言の欠落は起きていないが、D2プランが定めた「地図に出さずリスト表示のみの保留リスト」は未実装。今後ジオコーディングできない店を足すなら、①落とさずリストにだけ出す ②画面に「地図未掲載 N件」と明示する、のどちらかが要る。
- **飲食層は `matchesArea()`（物件用エリア判定）を使わない**: KL側が「他の名前付きエリアに入らないもの＝Mont Kiara」というキャッチオールで終わるため、Chinatown / Pudu / Imbi / Chow Kit の店が全部モントキアラ扱いになる。台帳の `area`（20値）を `fDiningArea` で完全一致させる方式にした。**副作用として、地図のエリアジャンプ（🏙️KL / 🏝️Penang バー）と飲食のエリアフィルタは連動しない**。統一するなら「台帳areaを物件エリアキーへマッピングする表」を作る判断が要る（要裁定）。
- **飲食店はKLのみ・ペナンは0件**: 台帳v9がKLの50店だけを持つため、ペナン側で飲食層に切り替えると常に0件になる。空結果メッセージは出るが、「この層にペナンのデータはまだ無い」とは言っていない。ペナン店の追加はデータ精度トラック（拡充方向の裁定2＝評判軸）。
- **カテゴリ大分類の「屋台街」は1件のまま**: プラン §裁定1 で8分類に確定済みだが、同§が「屋台街1件は分類として弱い（7分類案もある）」と論点を残している。今回は確定した8分類をそのまま実装しており、統合するかは未裁定。
- **`?layer=dining` 以外のURL状態（カテゴリ・価格帯などのフィルタ値）はURLに載せていない**: B3a-2から続く「URLにフィルタ値を載せるか未裁定」がそのまま飲食層にも当てはまる。
- **価格帯フィルタの基準は「夜の上限、夜が[0,0]なら昼の上限」**: `diningPriceCeiling()` に一本化し、価格帯フィルタ・並び替え（予算順）・サマリー「予算中央値」の3箇所が同じ関数を読む。**昼夜で価格帯が変わる店は夜側で分類される**（例: 昼RM80/夜RM450 は「RM400〜」）。昼だけで探したい需要が出たら軸を分ける判断が要る。
- **DOMレベルの回帰テストは今回も未整備**: matchesDining・価格帯・ヒーロー文字列・並び替え・カード・詳細パネルは純関数として test/diningLayer.test.js（55件）で押さえたが、「🍽飲食を押す→エリアselectがデータから生成される→地図のピンが立つ」の結線は実画面確認に依存。

## 2026-08-07 (D4 外食モード・個人記録)

**解消した既存項目**
- ✅ **D4（外食モード・個人記録UI）を実装**: 前回「一切作っていない」と記録した項目。訪問済トグル / 行きたい / 再訪3択 / 実額 / 感想（**複数行 textarea**）/ 訪問済み一覧（行った店ビュー）/ localStorage（`mkd_dining_personal_v1`）/ 書き出し（ファイルDL＋コピペ）/ 読み込み（統合・置き換え、**台帳v9の placeId キーを R#### へ変換**）/ 全消去（confirm2段）。必須要件①〜⑤が揃った（⑥はD3で完了）。
- ✅ **台帳v9の既知欠陥を移植時に潰した**: ①訪問日のUTCずれ→ローカル日付（`localDate()`）②描画が空レコードを50件作る→`getEntry()` は読むだけ ③ヘッダーとLogで実額平均の母集団が違う→**集計は全部「訪問済みのみ」に一本化**し、金額の集計は 行った店 ビュー1箇所だけに置いた ④条件フラグが単一選択→「行きたい」「未訪問」を独立トグル化（併用で「行きたいのにまだ行っていない店」が引ける）⑤内訳の二重丸め→印字するevを `total−au−ct` から出し、50件全部で合計一致をテスト ⑥インポートのキー未検証→未知キーを件数＋実キーで表示 ⑧エスケープ→`esc()` 一本。
- ✅ **exタグの文字列部分一致を廃止**: v9は `"Award"` を含むかどうかで加点していた（「2030 Service Award」のような新語が黙って加点される）。`EX_TAG_POINTS` のENUMにし、restaurants.json に未知のタグが増えたら `test/diningScore.test.js` が落ちる。

**今回スコープ外にしたもの（要裁定・後日）**
- **訪問日を手で直すUIが無い**: v9にも無く、`vd` は初回ONのときだけ自動で入る。「先週行ったのを今日登録した」場合、日付は今日になる。行った店ビューの並びは vd 降順なので、まとめて登録すると実際の順と食い違う。要裁定（D4計画の未決(d)を引き継ぎ）。
- **店ごとの記録削除が無い**: 全消去か、6項目を手で空にするかのどちらか。これもv9に無かったもの。
- **多端末同期は作らない（YAGNI継続）**: 端末をまたぐには「書き出し→貼り付け」が唯一の道。統合インポートは**足す方向にだけ効く**（ファイルが空にしている項目は既存値を消さない）ようにしたので、往復しても記録は減らない。消したいときは置き換え（confirm付き）。
- **「行った店」ビューに検索・絞り込みが無い**: v9も同じ。記録が数十件を超えたら要検討。
- **順位番号01〜（v9のカード左肩）は出していない**: D4計画の未決(c)。台帳スコア順が既定になったので順位＝スコア順であり、番号は同じ情報の二重表示になる。出すなら「総合点ソートのときだけ」。
- **`?mode=eatout` 以外の状態（どのビューを見ていたか・フィルタ値）はURLに載せていない**: B3a-2から続く未裁定項目と同じ。モード＋選択中の店は復元できる。
- **エリアジャンプのバーが一覧パネルの上に182px重なっている**: `.area-jump` が `.main` 全幅に `position:absolute` で乗るため、サマリータイルの TOTAL / SHOWN が隠れる。**D4以前からの既存事象**（住まい・外食どちらでも同じ182px）で今回は触っていない。直すなら `.area-jump` の `right` をパネル幅ぶん逃がすか、地図側だけに置く。
- **DOMレベルの自動回帰テストは今回も未整備**: スコア・集計・グループ化・インポート変換・エクスポート往復・ローカル日付は純関数として、モード分離／カード構造／モバイルCSSは契約テストとして押さえた（新規153件）。実際の結線は Chrome（localhost:5199）での手動確認で通した：モード切替・URL復元(`?mode=eatout&sel=`)・保存/リロード復元・v9形式インポート（placeId→R0001変換・未知キー1件を明示）・全消去2段・独立トグル併用・住まいモードに記録UIが1つも出ないこと。
- **ペナンの飲食店は0件のまま**: 台帳がKL50店のみ。外食モードでペナンに移動しても店は無い。

## 2026-08-07 (鮮度整理: 完成度監査で判明した解消済み項目)

監査（2026-08-07夜）で全項目をコードと突合した結果、以下は**解消済み**:
- ✅ 「Residential Only が常に0件」(B1) → B3で選択肢自体を撤去済み。KNOWN BUGコメントも置換済み
- ✅ 「schools_detail.json はペナン9校のみ」(B0) → 33/33校完備・契約テストで固定済み
- ✅ 「KL校23校の詳細は進行中」 → 完了
- ✅ 「SF_FEES 食い違い2点」(B3c) → 修正済み。**ただし新発見: Fairview Year6(age10)の外挿1点** → 同日削除済み（公表バンド外）
- 残る本物: SF_FEESとschools_detail.jsonの**二重管理そのもの**（チャート廃止かSF_FEES一本化かは要裁定のまま）
- 補足: 「DOMレベル回帰テスト未整備」は各バッチ末尾に重複記録されているが**1件の恒久課題**として読む

## 2026-08-07 (D6 台帳拡充 50→67店)

**実施**: 検証パス通過17店を追加（tools/dining-additions.json → converter が R0051〜R0067 として合成）。9分類目「カフェ・デザート」新設（裁定E-1）。台帳スコアの基準★は**v9由来の4.36に固定**（BASELINE_STAR。再計算だとTTDI Meat Pointの25,144件が加重の約6割を握り全店のスコアが動くため。改定は裁定事項）。

**保留・要観察**:
- **Place ID未取得2店**: Kin Gyu / MT Hotpot（`pending:` プレースホルダ。Mapsリンクは非表示にフォールバック）。Cotta / Napa Thai / Bocado は CID形式（`cid:`→10進変換でリンク生成）
- **Napa Thai**: 1人あたり実勢価格が未確定（[0,0]=予算要確認表示）・定休日はソース矛盾（要現地確認）
- **Cotta**: レビュー件数は「700+」の概数（wanderlogに個別ページなし・要更新）
- **値未取得で見送り**: Aposto / UMI(Imperial Lexis)（Google実値なし）・Taka Izakaya / Bait Bistro（Maps個別リスティング不明・閉店/改名疑い）
- **Wagyu Saikou**: Google Mapsで臨時休業表示 → 再開を確認できたら追加候補に戻す
- **KUKI Vegan**: ★4.7/183で新店例外の数値はクリアだが2023年11月開業（裁定時に「新店」該当性の確認を推奨）
- **kidOk に null（不明）を導入**: 詳細パネルは「—」表示。v9由来の0/1と三値になった

## 2026-08-07 (UX2 外食の改善2件: エリア連動・昼の予算)

**実施**: ①エリアジャンプが飲食層でも効くようになった（台帳の`area`ラベル24種とジャンプキーは対応しないので、**飛んだ中心からの半径3km**で絞る。チップ「近く: Mont Kiara（3km）」で解除）。②住まいモードの層タブに4つ目「🍽 飲食 ↗」＝外食モードへの入口（層にはならない・押すと `setLayer('dining')` が `setMode('eatout')` に読み替えられる既存の実装をそのまま踏む）。③「☀ 昼の予算」トグル＝価格帯・並び替え・予算中央値が読む基準を夜→昼に切り替える1つのフラグ。

**保留・要裁定**:
- **「近く」と「昼の予算」はURLに載っていない**: `?mode=eatout&sel=` は復元できるが、半径フィルタと予算基準は共有リンクに乗らない。既存の「フィルタ値をURLに載せていない」未裁定項目と同じ扱い（B3a-2から継続）。載せるなら`near=mont-kiara`のようにキーで持つのが素直（緯度経度をURLに書くと丸めと桁で壊れる）。
- **半径3kmは固定でユーザーが変えられない**: 実データで測って決めた値（KL7エリアで12〜31件・ParkCityのみ4件）。「2km/3km/5km」の選択肢を出すかは裁定事項。出すならチップの表記も可変にする必要がある。
- **ペナンのエリアで飲食を絞ると必ず0件**: 台帳にペナンの店が無いため（既存の保留項目と同根）。0件時は通常の空状態が出るだけで、「ペナンには台帳がない」とは言っていない。
- **「近く」を外しても地図は飛んだ先のまま**: チップの✕は一覧を広げるだけでナビゲーションは戻さない（`fArea`のチップと同じ挙動に揃えた）。地図まで戻すべきかは未裁定。
- **DOM結線は手動確認**: 純関数と`index.html`契約テストで押さえたが、ジャンプ→チップ表示→✕解除、トグルON→並び替えラベルが「（昼基準）」になる、の実クリックは Chrome での目視が要る（本バッチでは未実施）。
## 2026-08-07 (UX検証バッチ: 詳細パネル系5件 / feat/ux2-detail)

**保留（スコープ外・要裁定）**:
- **サマリータイルの日英併記は見送り**: 一覧パネル336px・タイル4分割＝1枚あたり文字領域は約73px（`--fs-label` 11px で日本語6.6文字）。「売買単価中央値 (PSF)」は約135pxで**中央値もPSFも省略される**ため、`PSF中央値`／`NLA中央値` のまま据え置いた（タイル幅優先の判断）。日英併記が要るなら、タイルを2×2に組み替えるか、ホバー/長押しで説明を出す別案が要る。
- **モバイル(≤390px)の選択時パン**: 詳細パネルは 78vw(最大280px)×60vh を地図の左上に被せるため、パン用の余白を画面幅の60%に丸めている（`panPaddingFor`）。結果としてピンがパネルの下に残る端末幅がありうる。実機で気になるなら「モバイルでは選択時に地図を動かさない」か「パネルを下端に寄せる」のどちらかを裁定したい。
- **Luxuryスコアの但し書き**: 「独自算出」だけをラベルに出し、全文（100点満点・公表値ではない）は `title` ツールチップに置いた。タッチ端末ではツールチップが出ないので、恒久的には ? アイコン等の明示的な説明UIが要る。

## 2026-08-07 D7発見バッチ・見送り店（理由つき）
- **ROCKU Yakiniku (Pavilion)** 4.6×3,948 — Klang Valley 4-5店でチェーン規則(2-4店)超過。エンタメ型ビュッフェで業態も特殊。再検討余地あり
- **Yeast Bistronomy (Bangsar)** 4.6×2,528 — Yeast Groupとして約5拠点でチェーン規則超過
- **Maria's SteakCafe (Bangsar)** 4.5×1,017 — 3店以上のカジュアルチェーン。バーは通過、選抜密度の観点で次点
- **Seirock-ya Ramen (Uptown)** 4.5×1,546 — 店舗数の情報相反(2店 vs ハラール認証8店)。裏取りできるまで保留
- **Pho King (SS2)** 4.6×1,904 — TripAdvisor 3.8との乖離+同名店混同リスク。住所153 Jalan SS2/6の同定確認まで保留
- **Uo Shin Hartamas** — Grab 4.6×100件強で件数バー(300+)未達の可能性大。Google実値確認まで保留
- **Jojo Little Kitchen** — 11拠点の大規模チェーン+代替指標(RG3.8/TA4.0)がバー未達。除外
- **Inside Scoop (Bangsar)** 4.5×718 — 全国規模チェーンのため対象外
- **In House Restaurant (Publika)** 4.8×964 — 件数1,000にわずかに届かず。閾値スレスレのため将来の再チェック候補
- **Sushi Taka / KUKI等 G1権威系の裁定** — G1収集完了後に判定を追記

- 【現況注記 2026-08-07深夜】上記B3a保留の「屋台街は8分類のまま」の母数は現在9分類(カフェ・デザート新設後)。屋台街=1件を独立分類のままにするかの論点自体は引き続き未裁定

## 2026-08-08未明 夜間リファクタ+監査で保留にした課題
**裁定が要るもの(夜間の独断で触らなかった):**
- **tier(継続性)の付与ルール**: 拡充43店は全てtier=0で継続性0点。Bijan(2003年)ですら0扱いで、新規店が構造的にスコア下位に沈む(新26店平均39.4 vs 旧67店53.9)。「tierはv9の50店専用」と明文化するか、創業年からの付与規則を作るか
- **vox空欄の表示**: 43/93店でvox(支持/不満)が空→詳細に「—」セクション2つ。現仕様「両方常時表示」は明示テストで固定された意図的契約のため夜間では覆さず。推奨=両方空なら節ごと非表示(片方だけ表示は引き続き禁止)
- **--type-condoの正**: CSSトークン(=accent青)とJS(MARKER_COLORS.condo=スレート#78909c)が乖離。物件色は3系統(帯/クラスタ/ピン)ありどれを正とするかは設計裁定
- **トグルUIの表駆動化(S1)**: 1トグル=最大11箇所編集の現状をTOGGLES表1行に。挙動同一だがDOM配線のテストが無いため夜間見送り
- **層レジストリ化(S10)**: 層の知識が11テーブルに分散(第5の層を足すと20箇所)。LAYER_SPEC一本化はnearby.jsの方式を一般化する設計仕事
**小粒の残債(次の区切りで):**
- schoolFinderの物件tooltipにupcoming/価格ガードなし(現データでは発火しない潜在。cardHeroText共用が筋)
- Niko Neko Bangsarの通り(住所=Jalan Telawi vs 座標=Lorong Maarof)の実地裏取り
- エリア名の(PJ)表記ゆれ(Damansara Utamaだけ無印)・Positanoの「KL Metropolis」vs実体Publika
- natCodeは読み込むだけの死列(描画なし)。使うか消すか
- pending placeId 9件(Nero Nero/Vantador/Der Backmeister/Foo Foo/Niko Neko/Dancing Fish/宮武/Jie/Shu)
- list.js(928行)の分割候補: format.js抽出は完了。次はcards.js(約225行・純関数)とsummary.js

## 2026-08-08 ミシュランKL完全網羅で判明した保留
- **Qureshi (KLGCC)** — 2026年版Selected発表リストに載ったが、1年超の臨時休業で公式サイトからページ削除済み(「Restaurant not found」実確認)。訪問不能のため台帳追加を見送り。再開したら追加(Wagyu Saikouと同じ休業ウォッチ)
- **Heun Kee Claypot / Kappo Hiyori** — 2026年版で選外を確認しmichelinバッジを外した(店は台帳に残置・経緯は各editorNoteに記載)
- 新規追加分のGoogle Place IDは全て pending:(Places API無しでは取得不能)。既存のpending 9件と合わせ、いつかまとめてブラウザ実機で採取する

## 2026-08-09 新Google条件再調査(★4.2-4.9未満×1,000件+)で保留にしたもの

- **One Dish One Taste 重慶火鍋(Desa ParkCity ★4.8×4,244)**: チェーン規則の裁定待ち。中国語ブランド「一碟一味」で数えると5店(=除外)、英語名だけなら3店(=採用可)。ブランド同一性の判定基準を竹森さんに要裁定
- **Moggumung BBQ(Hartamas ★4.8)**: 件数の食い違い(調査時1,172 vs 二次確認679)。Google直接照会で1,000件以上を確認できれば復活可
- **PULP by Papa Palheta**: APW Bangsarを2026-05-31退去・移転先未発表。確定したら再検討
- **Fatty Crab(Taman Megah ★4.2×4,505)**: 全数値が条件合格だが担当ゾーン外のポケット。次回スイープで判定
- **Maria's Signature / Spice Guys BLR(Suria KLCC・★4.8/4.7×1,700-1,800)**: チェーン店舗数が未確認のためグレー保留
- **北KL圏(Kepong/Cheras/OKR)とKeramat内陸・Titiwangsa東・Ampang Hilirの★4.2-4.4帯**: 主力出典makan.guruが★4.5以上限定のため未探索。この帯の大衆中華・ホーカーは多い。次回スイープでGoogle直接照会の追加パス要
- **K KL（圭）(★4.9×35・ミシュランSelected)**: 「4.9は削除」裁定に対しミシュラン完全網羅裁定を優先して残留させた(AI判断)。外すべきなら竹森さんの指示で削除
- **既存台帳の口コミ1,000件未満の大衆系(MK再調査組のSeng Kee/Muska/Casa Rosa等)**: 新条件の遡及適用は未裁定のため残置中
- **Hot Bird(Damansara Utama ★4.8×11,820)**: 6店舗チェーンで機械除外。数値は文句なしの合格のため「チェーン基準を6店まで緩める」裁定なら再検討価値大
- **中東カテゴリの独立**: 再調査で中東系が8店に増加(現在は「日本・その他アジア」に同居)。カテゴリ独立は竹森さんの裁定事項
